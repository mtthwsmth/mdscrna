% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mds_dge_split.R
\name{mds_dge_split}
\alias{mds_dge_split}
\title{Split DGE results into up- and down-regulated sets}
\usage{
mds_dge_split(
  result,
  foldChangeName = "logFC",
  foldChangeCutoff = (1.5),
  pvalName = "FDR",
  pvalCutoff = 0.05,
  expressionCutoffName = "logCPM",
  expressionCutoff = NULL
)
}
\arguments{
\item{result}{A data.frame-like object with one row per gene.}

\item{foldChangeName}{Column name containing log2 fold-change.}

\item{foldChangeCutoff}{Fold-change cutoff in linear space (e.g. 1.5).}

\item{pvalName}{Column name containing adjusted p-values (e.g. "padj" or "FDR").}

\item{pvalCutoff}{Adjusted p-value cutoff (e.g. 0.05).}

\item{expressionCutoffName}{Column name containing an expression metric (e.g. "logCPM").}

\item{expressionCutoff}{Optional numeric cutoff for the expression metric; if NULL, no expression filtering is applied.}
}
\value{
A list with three elements: \code{res}, \code{up}, and \code{down}.
}
\description{
Given a DESeq2 or edgeR results table, filters by fold-change and adjusted
p-value and returns the full filtered table plus up/down subsets.
Fold-change cutoff is specified in linear space and converted to log2.
Rows with NA values are removed before filtering.
}
