% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mds_enrichgo_up_down.R
\name{mds_enrichgo_up_down}
\alias{mds_enrichgo_up_down}
\title{Run GO enrichment on up/down gene sets}
\usage{
mds_enrichgo_up_down(df.ls, ontology = "BP", universe, OrgDb, ...)
}
\arguments{
\item{df.ls}{A named list containing \code{up} and \code{down} data.frames. Row names
should be gene identifiers (SYMBOL by default).}

\item{ontology}{GO ontology to use. One of \code{"BP"}, \code{"MF"}, or \code{"CC"}.
Default \code{"BP"}.}

\item{universe}{Character vector of gene IDs defining the background universe.}

\item{OrgDb}{An \code{OrgDb} object (e.g. \code{org.Hs.eg.db::org.Hs.eg.db}).}

\item{...}{Additional arguments passed to \code{clusterProfiler::enrichGO()}.}
}
\value{
A named list containing \code{ego.up} and/or \code{ego.down}. If the corresponding
input set is empty, that element is omitted.
}
\description{
Runs \code{clusterProfiler::enrichGO()} on the \code{up} and \code{down} results in a
DGE split list (e.g. output of \code{mds_dge_split}). If any terms are enriched,
the result is augmented with \code{enrichplot::pairwise_termsim()}.
}
