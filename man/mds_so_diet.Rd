% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mds_so_diet.R
\name{mds_so_diet}
\alias{mds_so_diet}
\title{Clean a Seurat object down to RNA counts + (filtered) meta.data}
\usage{
mds_so_diet(
  so,
  assay = "RNA",
  layer = "counts",
  remove_clustering = TRUE,
  clustering_prefixes = c("SCT_snn_res", "RNA_snn_res"),
  additional_remove_patterns = NULL,
  verbose = TRUE
)
}
\arguments{
\item{so}{A Seurat object.}

\item{assay}{Assay to extract counts from. Default \code{"RNA"}.}

\item{layer}{Which layer to treat as counts. Default \code{"counts"}.}

\item{remove_clustering}{Logical; remove clustering columns from meta.data. Default \code{TRUE}.}

\item{clustering_prefixes}{Character vector of clustering prefixes to remove. Default
\code{c("SCT_snn_res", "RNA_snn_res")}.}

\item{additional_remove_patterns}{Optional character vector of additional regex patterns.}

\item{verbose}{Logical; emit messages. Default \code{TRUE}.}
}
\value{
A new Seurat object with counts and cleaned meta.data.
}
\description{
Creates a new Seurat object containing only the specified assay counts layer and a cleaned
meta.data table (optionally removing clustering columns).
}
\details{
If the requested layer (default \code{"counts"}) is not present but split layers are detected
(e.g. \code{counts.sample1}, \code{counts.sample2}), the function runs
\code{SeuratObject::JoinLayers()} internally to create the requested layer and prints a message.
}
