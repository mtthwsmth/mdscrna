% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mds_so_plot_coldata.R
\name{mds_so_plot_coldata}
\alias{mds_so_plot_coldata}
\title{Plot Seurat meta.data with scater::plotColData}
\usage{
mds_so_plot_coldata(
  seurat_obj,
  x = NULL,
  y,
  colour_by = NULL,
  x_angle = 0,
  combine = TRUE,
  ncol = NULL,
  guides = "collect",
  plot_titles = NULL,
  ...
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{x}{Optional metadata column name used for the x-axis.}

\item{y}{One or more metadata column names used for the y-axis.}

\item{colour_by}{Optional metadata column name used for colouring.}

\item{x_angle}{Numeric; rotation angle for x-axis labels. Default 0.}

\item{combine}{Logical; if TRUE and \code{patchwork} is installed, combine multiple plots.}

\item{ncol}{Number of columns when combining plots (passed to \code{patchwork::wrap_plots}).}

\item{guides}{Guide collection behavior when combining plots (passed to \code{patchwork::wrap_plots}).}

\item{plot_titles}{Optional character vector of titles per \code{y}.}

\item{...}{Additional arguments passed to \code{scater::plotColData()}.}
}
\value{
A \code{ggplot} object if \code{length(y) == 1}, otherwise a combined patchwork plot
(if enabled and available) or a list of \code{ggplot} objects.
}
\description{
Convenience wrapper that makes Seurat metadata easy to plot using the
\code{scater::plotColData()} interface (i.e., \code{x}, \code{y}, and \code{colour_by}
aesthetics taken from \code{colData}). This function converts the Seurat object's
\code{@meta.data} into a minimal \code{SingleCellExperiment} and then calls
\code{scater::plotColData()}.
}
\details{
This function constructs a temporary \code{SingleCellExperiment} (including a dummy assay)
solely to reuse \code{scater::plotColData()}. There must be a better way to do this
using equivalent \code{ggplot2} but all of my attempts so far look way worse
than the ones generated by \code{scater}
}
