% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mds_so_set_union_vf.R
\name{mds_so_set_union_vf}
\alias{mds_so_set_union_vf}
\title{Union variable features across groups for Harmony-style workflows}
\usage{
mds_so_set_union_vf(
  so,
  split_by,
  nfeatures = 3000,
  selection.method = "vst",
  assay = NULL,
  ret = "object",
  verbose = TRUE
)
}
\arguments{
\item{so}{A Seurat object.}

\item{split_by}{Metadata column name used to split cells into groups.}

\item{nfeatures}{Number of variable features to select per group. Default 3000.}

\item{selection.method}{Variable feature selection method passed to
\code{Seurat::FindVariableFeatures()}. Default \code{"vst"}.}

\item{assay}{Optional assay name to set as the default assay before selecting features.
If \code{NULL}, uses the current default assay.}

\item{ret}{Return mode: \code{"object"} returns the Seurat object with union features set;
\code{"features"} returns the union feature vector. Default \code{"object"}.}

\item{verbose}{Logical; print messages. Default \code{TRUE}.}
}
\value{
If \code{ret = "object"}, a Seurat object with \code{VariableFeatures(so)}
set to the union of group-wise variable features. If \code{ret = "features"},
a character vector of feature names.
}
\description{
Computes variable features separately within each group defined by \code{split_by},
takes the union across groups, and either assigns them to the Seurat object
(\code{VariableFeatures(so) <- union_features}) or returns the feature vector.
}
\details{
This follows the approach used in the Harmony Seurat v5 guide: split cells by a
metadata field, run \code{FindVariableFeatures()} per group, take \code{unique(unlist(...))}
and assign the union back to \code{VariableFeatures(object)} before downstream steps
like scaling, PCA, and \code{RunHarmony()}.  \href{https://immunogenomics.r-universe.dev/harmony/doc/Seurat.html}{oai_citation:0‡immunogenomics.r-universe.dev}

#' @details
This helper does not run normalization. For typical workflows, run
\code{Seurat::NormalizeData()} (and any required preprocessing) before calling this
function so \code{FindVariableFeatures()} operates on the intended data.
}
\seealso{
\code{\link[Seurat:FindVariableFeatures]{Seurat::FindVariableFeatures}},
\code{\link[harmony:RunHarmony]{harmony::RunHarmony}}
}
